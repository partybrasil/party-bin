// Highlight.js ya se carga por CDN en base.html
// Aquí puedes agregar utilidades JS para el contador, copiar al portapapeles, etc.
function copyToClipboard(text) {
    navigator.clipboard.writeText(text);
}
